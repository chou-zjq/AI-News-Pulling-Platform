# AI 新闻调取平台 — 后端依赖
# Python 3.11+

# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0

# 异步 HTTP 客户端（新闻抓取）
httpx==0.28.1

# 加密（KeyBox KeyVault）
cryptography==44.0.0

# 模板引擎（Markdown 记录本生成）
jinja2==3.1.4

# 环境变量管理
python-dotenv==1.0.1

# 开发依赖
pytest==8.3.4
pytest-asyncio==0.25.0
